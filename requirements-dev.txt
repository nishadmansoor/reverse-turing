# Training, data preparation, evaluation, and model export.
# Install these to retrain models, rebuild artifacts, or regenerate the
# quantized ONNX graphs. The deployed app needs none of them.

-r requirements.txt

# Training backend
torch>=2.0.0

# ONNX export + quantization (src/quantize.py)
onnx>=1.15.0
onnxscript>=0.1.0

# Data pipeline
pandas>=2.0.0
datasets>=2.14.0
pyyaml>=6.0
requests>=2.31.0

# Training loop
tqdm>=4.65.0

# Evaluation & plots
matplotlib>=3.7.0
seaborn>=0.12.0

# Notebooks
jupyter>=1.0.0
ipykernel>=6.0.0
