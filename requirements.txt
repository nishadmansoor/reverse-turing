# Runtime dependencies — what the deployed app actually imports.
# Training and data-prep dependencies live in requirements-dev.txt.
#
# Note there is no torch here. The app serves int8 ONNX graphs through
# onnxruntime (~50 MB) instead of the torch runtime (~800 MB unpacked), which
# is what lets the container fit a free hosting tier. torch is still required
# to train and to export the graphs — see requirements-dev.txt.

# Web
flask>=3.0.0
gunicorn>=21.2.0

# Inference
onnxruntime>=1.17.0

# Tokenizer only — transformers is pure Python here and pulls in `tokenizers`,
# not a deep-learning backend.
transformers>=4.30.0
tokenizers>=0.13.0

# Feature extraction and the linear models
scikit-learn>=1.3.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
