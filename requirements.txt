# Runtime dependencies — what the deployed app actually imports.
# Training and data-prep dependencies live in requirements-dev.txt.
#
# Install CPU-only torch for deployment:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

# Web
flask>=3.0.0
gunicorn>=21.2.0

# Inference
torch>=2.0.0
transformers>=4.30.0
tokenizers>=0.13.0

# Feature extraction and the linear models
scikit-learn>=1.3.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
